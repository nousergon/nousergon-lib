{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nousergon.ai/schemas/alpha-engine/producer_champion_audit.v2.json",
  "title": "producer_champion apply-audit artifact (v2 \u2014 weekly winner-take-all)",
  "description": "FROZEN contract for config/apply_audit/producer_champion/{date}.json (+ latest.json mirror) \u2014 produced by crucible-backtester optimizer/champion_promotion.py at the end of EVERY weekly Evaluator run, mirroring the config#1841 config/apply_audit/ per-optimizer pattern. Written REGARDLESS of outcome (promoted / no_contest / unchanged_winner_already_champion / error) \u2014 the config#2054 lesson is binding: a pointer-writer's liveness must be provable from day one, so this weekly write IS the liveness/freshness proxy for the champion-promotion engine (a correctly-held no-contest week does not touch config/producer_champion.json, so pointer mtime alone cannot prove the engine is alive). v2 (2026-07-14, alpha-engine-config-I2518): the champion-promotion policy moved from a HAC-significance/2-week-hysteresis/2-week-cooldown gated engine to weekly winner-take-all (Brian's ruling) \u2014 the v1 fields `challenger_matured_cohorts`, `sn_lift_vs_champion`, `consecutive_wins`, `cooldown_until` are RETIRED (no longer meaningful under winner-take-all, which carries no state forward between weeks) and replaced by `champion_score`/`challenger_score`. No live consumer of this artifact's fields exists outside this repo (verified 2026-07-14 by repo-wide grep across the dashboard/evaluator/executor repos), so this is a clean version bump requiring no cross-repo coordination. v1 records already written to S3 (schema_version=1) remain valid HISTORICAL documents under the frozen v1 shape (recoverable via this file's git history) and are never revalidated against v2 \u2014 new records always write schema_version=2. Additive-only evolution going forward: consumers MUST tolerate unknown extra fields; further renames/removals require another schema_version bump. ADDITIVE (same day, alpha-engine-config-I2544): `leaderboard_date_used` records which research/producer_leaderboard/{date}.json artifact was actually consulted this run, since the gate now reads the LATEST AVAILABLE leaderboard <= date (not necessarily an exact same-day match \u2014 see optimizer/champion_promotion.py module docstring) and the `leaderboard_stale_gt_8d` blocked_by slug can now fire. Optional (not in `required`) precisely so v2 records already written earlier today, before this addition, remain valid without a version bump \u2014 per this same additive-only policy.",
  "type": "object",
  "required": [
    "schema_version",
    "date",
    "outcome",
    "champion_before",
    "champion_after",
    "champion_score",
    "challenger_score",
    "blocked_by",
    "freeze"
  ],
  "properties": {
    "schema_version": {
      "const": 2
    },
    "date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Evaluator run date (trading-day normalized) \u2014 matches the dated S3 key."
    },
    "generated_at": {
      "type": "string",
      "description": "UTC ISO-8601 wall-clock write time."
    },
    "outcome": {
      "enum": [
        "promoted",
        "no_contest",
        "unchanged_winner_already_champion",
        "held_shadow_only",
        "error",
        "demoted",
        "held_insufficient_data",
        "held_cooldown",
        "held_not_significant"
      ],
      "description": "promoted = the pointer was moved this run (champion_before != champion_after) \u2014 the challenger's realized weekly score beat the champion's. no_contest = either arm's weekly score was unavailable this week (validity guard, e.g. thinktank_coverage not yet in the leaderboard, no resolved outcomes, or the leaderboard artifact missing/stale) \u2014 a definitional hold, never a default win for either side. unchanged_winner_already_champion = both scores were available and the incumbent champion still had the higher score, or scores tied. held_shadow_only (ADDITIVE, alpha-engine-config-I2515, Brian's 2026-08-20 shadow-only ruling) = both arms' weekly scores were available and the CHALLENGER scored strictly higher, but that arm is declared shadow-only in crucible-backtester optimizer/champion_promotion.py::SHADOW_ONLY_ARMS \u2014 measured every cycle, never promoted \u2014 so the live config/producer_champion.json pointer was deliberately HELD. Deliberately a distinct outcome rather than a reuse of an existing one: no_contest asserts no comparable evidence existed (false here \u2014 there WAS a contest and the shadow arm won it) and unchanged_winner_already_champion asserts the incumbent scored highest (also false here); either would erase the very counterfactual shadow mode exists to measure. The `counterfactual_winner` field names the arm that actually won. error = the engine could not complete evaluation this run (e.g. an unexpected exception during scoring) \u2014 the failure also surfaces via the pipeline's completeness tracker; this record is the durable trace. `demoted`/`held_insufficient_data`/`held_cooldown`/`held_not_significant` are RETIRED (pre-2026-07-14 HAC/hysteresis/cooldown engine) \u2014 read-tolerated for historical v1 records, never written by the current engine."
    },
    "champion_before": {
      "enum": [
        "agentic",
        "scanner_predictor_direct",
        "thinktank_coverage",
        "scanner_top20_predictor",
        "no_agent_quant",
        "single_agent_quant",
        null
      ],
      "description": "The pointer's champion value read at the start of this evaluation (or the base-case default 'scanner_predictor_direct' when no pointer has ever been written). 'agentic' is READ-TOLERATED for historical continuity (the 2026-07-13 bootstrap's own audit record carries champion_before='agentic') but is WRITE-FORBIDDEN going forward \u2014 see contracts/producer_champion.schema.json. Null only on outcome='error' when evaluation aborted before the pointer could even be read."
    },
    "champion_after": {
      "enum": [
        "agentic",
        "scanner_predictor_direct",
        "thinktank_coverage",
        "scanner_top20_predictor",
        "no_agent_quant",
        "single_agent_quant",
        null
      ],
      "description": "The pointer's champion value at the end of this evaluation. Equal to champion_before on every no_contest/unchanged_winner_already_champion/error outcome, and on a frozen promotion (blocked_by=['frozen']) \u2014 this artifact records a held week without ever mutating config/producer_champion.json. Null only alongside a null champion_before (outcome='error', pointer never read)."
    },
    "champion_score": {
      "type": [
        "number",
        "null"
      ],
      "description": "champion_before arm's realized weekly top-N alpha lift vs the shared agentic-pipeline baseline (see champion_promotion.py module docstring \u2014 indirect/common-comparator comparison). Null when unavailable this week (no_contest, or outcome='error')."
    },
    "challenger_score": {
      "type": [
        "number",
        "null"
      ],
      "description": "The challenger arm's (VALID_CHAMPIONS complement of champion_before) realized weekly top-N alpha lift vs the same shared baseline. Null when unavailable this week (no_contest, or outcome='error')."
    },
    "blocked_by": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "array",
          "minItems": 1,
          "items": {
            "enum": [
              "no_valid_scanner_predictor_direct_selections",
              "no_valid_thinktank_coverage_selections",
              "scanner_predictor_direct_counterfactual_unavailable",
              "thinktank_coverage_not_in_leaderboard",
              "thinktank_coverage_no_resolved_outcomes",
              "leaderboard_unavailable",
              "leaderboard_stale_gt_8d",
              "arm_score_unavailable",
              "shadow_only_arm",
              "frozen",
              "unclassified_error",
              "insufficient_matured_cohorts",
              "cooldown_active",
              "not_significant_hac_adjusted",
              "hysteresis_not_satisfied",
              "leaderboard_stale",
              "feed_producer_dead",
              "thinktank_coverage_thin_evidence",
              "thinktank_coverage_confidence_unknown",
              "scanner_predictor_direct_thin_evidence",
              "scanner_predictor_direct_confidence_unknown",
              "leaderboard_horizon_mismatch",
              "scanner_top20_predictor_counterfactual_unavailable",
              "scanner_top20_predictor_thin_evidence",
              "scanner_top20_predictor_confidence_unknown"
            ]
          }
        }
      ],
      "description": "Stable slugs naming why the pointer did not move this run (non-null only on no_contest/frozen-promoted/error outcomes; null on promoted-non-frozen/unchanged_winner_already_champion). thinktank_coverage_not_in_leaderboard/thinktank_coverage_no_resolved_outcomes/scanner_predictor_direct_counterfactual_unavailable/leaderboard_unavailable/arm_score_unavailable/no_valid_*_selections are the current winner-take-all validity-guard slugs (config-I2518). leaderboard_stale_gt_8d (config-I2544, 2026-07-14) = the latest research/producer_leaderboard/{date}.json available at or before `date` is more than 8 calendar days older than `date` \u2014 the gate now reads the LATEST AVAILABLE leaderboard (not an exact same-day match, since the writer is an async advisory child SF that may lag or fail), but still refuses to score against evidence this stale. feed_producer_dead (alpha-engine-config-I3165, 2026-07-23) = the arm that would become champion this run (the challenger, on a would-be promotion) declares one or more `feed_dependencies` (see that field) whose producer looked dead/orphaned at promotion time (probed via the ARTIFACT_REGISTRY-declared freshness rule for that artifact_id, e.g. `optimizer/champion_promotion.py`'s reuse of `analysis/scanner_predictor_research_free_backfill.py::assert_champion_feed_fresh` for `research_free_backfill`) \u2014 the promotion-time complement to the config-I3086 `critical_while_champion_arm` ONGOING-monitoring mechanism (config#3053/config#3165: the 2026-07-20 no-trade morning, where `scanner_predictor_direct`'s feed chain was silently orphaned 2026-07-14 with nothing at promotion time checking it). Degrades a would-be promotion to no_contest exactly like every other validity guard here \u2014 never crashes the run, never silently promotes through a dead feed. thinktank_coverage_thin_evidence / thinktank_coverage_confidence_unknown / leaderboard_horizon_mismatch (alpha-engine-config-I7549, 2026-08-17) = the evidence-confidence gate. thinktank_coverage_thin_evidence: the producer-leaderboard row for the arm carried confidence=\"thin\" (scored, but on fewer than the artifact's min_dates_for_inference dates, produced by crucible-research scoring/leaderboard_scoring.py::confidence_for under alpha-engine-config-I7542) \u2014 the mean exists and is honest but its own dispersion is undefined (se/t_stat null at n<5), so it is not comparable against the other arm and the pointer is held. Deliberately DISTINCT from thinktank_coverage_no_resolved_outcomes (confidence=\"insufficient\", nothing scored at all): thin means WAIT for the cohort to mature, insufficient means GO LOOK at the producer. Before I7549 the gate accepted any truthy n_dates_scored, so a one-date mean with a null SE and a null t-stat could move the live champion pointer (live example: research/producer_leaderboard/2026-08-14.json, thinktank_coverage at n_dates_scored=1). thinktank_coverage_confidence_unknown: the artifact carried no `confidence` key and no `min_dates_for_inference` floor to derive one from (a pre-I7542 artifact), or carried a `confidence` value outside the known vocabulary \u2014 refused rather than trusted, since an old artifact read as confident is the same defect. leaderboard_horizon_mismatch: the artifact's top-level `horizon_days` disagrees with optimizer/champion_promotion.py::GATE_HORIZON_DAYS (21 trading sessions, the leaderboard's PRIMARY horizon and the only one at which BOTH arms are scored) \u2014 an upstream primary-horizon change surfaces as a loud no-contest instead of silently rescoring the champion gate. All three degrade to no_contest exactly like every other validity guard: fail-STATIC, never a flip the other way. scanner_predictor_direct_thin_evidence / scanner_predictor_direct_confidence_unknown (alpha-engine-config-I7549, champion-side half) are the same two verdicts on the OTHER arm of the same comparison: the scanner_then_predictor_topN counterfactual returns a result at n_cycles >= 1, so the arm the gate compares the leaderboard row AGAINST could itself be a single observation. thin = scored on fewer than optimizer/champion_promotion.py::MIN_CYCLES_FOR_INFERENCE cycles (this repo declares that floor because this repo owns that arm's measurement; the leaderboard-scored arm's floor is read off the artifact and never reimplemented \u2014 champion-challenger-policy.md \u00a710). confidence_unknown = the counterfactual reported a mean with no usable n_cycles behind it. Per-arm slugs rather than shared ones because the operator response differs by arm: one waits on crucible-research's cohort, the other on this repo's research.db cycles. shadow_only_arm (ADDITIVE, alpha-engine-config-I2515, 2026-08-20) = paired with outcome=held_shadow_only: the arm that won on score this week is declared shadow-only (measured, never promoted) per Brian's 2026-08-20 ruling, so the pointer was HELD. Promoting such an arm requires its own separate ruling plus removing it from SHADOW_ONLY_ARMS; crucible-backtester's write_champion_pointer enforces the same rule independently and raises, so a caller bypassing the gate cannot move the pointer onto a shadow arm either. frozen = --freeze suppressed the write even though the challenger won. unclassified_error = outcome=error with no more specific reason. insufficient_matured_cohorts/cooldown_active/not_significant_hac_adjusted/hysteresis_not_satisfied are RETIRED (pre-2026-07-14 HAC/hysteresis/cooldown engine) \u2014 read-tolerated for historical v1 records only. leaderboard_stale is RETIRED (pre-I2544 exact-date-only read, same day as the v2 bump) \u2014 fired when the artifact's self-reported date disagreed with an exact-match run_date key; the latest-available read no longer requires an exact match, so this condition can no longer occur (superseded by leaderboard_stale_gt_8d for the age-bound case) \u2014 read-tolerated only."
    },
    "challenger": {
      "enum": [
        "agentic",
        "scanner_predictor_direct",
        "thinktank_coverage",
        "scanner_top20_predictor",
        "no_agent_quant",
        "single_agent_quant",
        null
      ],
      "description": "The BEST-SCORING challenger this run \u2014 the only arm that could have taken the seat. Was 'the complement of champion_before' under the two-arm set; alpha-engine-config-I8756 (Brian's 2026-08-27 ruling) made the slot N-arm, so the complement is no longer a single value. null when no challenger was scored this week, which is a no-contest. 'agentic' is read-tolerated for historical v1 records only."
    },
    "freeze": {
      "type": "boolean",
      "description": "Whether this run was invoked with --freeze. When true, the pointer write is always suppressed even if the challenger won this week (this record is still written unconditionally)."
    },
    "leaderboard_date_used": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ADDITIVE (alpha-engine-config-I2544, 2026-07-14). The date of the research/producer_leaderboard/{date}.json artifact actually consulted this run \u2014 the latest available <= `date` (see optimizer/champion_promotion.py::find_latest_research_producer_leaderboard_date), or null when no artifact <= `date` was available at all, or when evaluation aborted before scoring (outcome=error). Optional/nullable so pre-I2544 v2 records (written earlier the same day, before this field existed) remain valid without a version bump. Always present (never omitted) on any record built by the current engine, on every outcome including error."
    },
    "arm_confidence": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": {
        "type": "string"
      },
      "description": "ADDITIVE (alpha-engine-config-I7549, 2026-08-17). Per-arm evidence verdict for this run: {\"scanner_predictor_direct\": \"ok\"|\"thin\"|\"insufficient\"|\"unknown\", \"thinktank_coverage\": \"ok\"|\"thin\"|\"insufficient\"|\"unknown\"|\"unrecognised\"}. The first three values are crucible-research's own per-spec `confidence` read straight off the producer-leaderboard row (scoring/leaderboard_scoring.py::confidence_for, alpha-engine-config-I7542) against the slot's registered min_dates_for_inference floor; `unknown`/`unrecognised` are this repo's refusal verdicts for an artifact that could not supply a usable one. scanner_predictor_direct is scored from this repo's own scanner_then_predictor_topN counterfactual rather than the leaderboard, and reports the SAME vocabulary against optimizer/champion_promotion.py::MIN_CYCLES_FOR_INFERENCE (alpha-engine-config-I7549 champion-side half) \u2014 a two-arm gate whose arms grade their evidence in different vocabularies cannot be read as a comparison. `not_leaderboard_scored` was emitted for that arm between PR688 and the champion-side change and remains READ-TOLERATED for any record written in that window; nothing emits it now. Records WHICH EVIDENCE declined to decide, not merely that something did (champion-challenger-policy.md \u00a77.2) \u2014 `blocked_by` alone cannot distinguish a thin row from an absent one once both slugs exist. Null only on outcome=\"error\" (evaluation aborted before scoring). Values are deliberately open (`additionalProperties: string`, no enum) so a producer-side vocabulary addition does not invalidate records this repo already refuses to score. Optional (not in `required`) so pre-I7549 v2 records remain valid without a version bump \u2014 per this schema's additive-only policy."
    },
    "counterfactual_winner": {
      "enum": [
        "agentic",
        "scanner_predictor_direct",
        "thinktank_coverage",
        "scanner_top20_predictor",
        "no_agent_quant",
        "single_agent_quant",
        null
      ],
      "description": "ADDITIVE (alpha-engine-config-I2515, 2026-08-20). The arm with the strictly higher realized weekly score \u2014 who WOULD have taken the live pointer on score alone \u2014 or null when no comparison was possible (outcome=no_contest, or outcome=error where evaluation aborted before scoring). Equals `champion_after` on an ordinary promotion and `champion_before` on a defended incumbency; the case this field exists for is outcome=held_shadow_only, where it names the SHADOW-ONLY arm that won and was deliberately NOT promoted. Without it a held_shadow_only record could not express \"the shadow arm would have won\", and a shadow arm whose wins stop being visible in the durable record defeats the purpose of shadow mode (champion-challenger-policy.md \u00a73: measurement is unconditional and is not what promotion governs). Optional (not in `required`) so pre-I2515 v2 records remain valid without a version bump \u2014 per this schema's additive-only policy."
    },
    "feed_dependencies": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      },
      "description": "ADDITIVE (alpha-engine-config-I3165, 2026-07-23). The `champion_after` arm's declared upstream live-trade feed artifact_id(s) \u2014 e.g. `[\"research_free_backfill\"]` for `scanner_predictor_direct` \u2014 read from optimizer/champion_promotion.py::ARM_FEED_DEPENDENCIES, the static arm\u2192feed source of truth this same change introduces. Each named id is an ARTIFACT_REGISTRY.yaml artifact_id (alpha-engine-config/private-docs/ARTIFACT_REGISTRY.yaml) whose producer this gate probed for liveness at promotion time before allowing a flip onto that arm (see `blocked_by`'s `feed_producer_dead` slug) \u2014 the promotion-time complement to the config-I3086 `critical_while_champion_arm` ongoing-monitoring mechanism, closing the config#3053 gap where nothing checked the feed chain AT the moment of promotion. Null/empty when `champion_after` declares no feed dependency (e.g. `thinktank_coverage`, whose evidence chain is the producer leaderboard already gated by `leaderboard_date_used`/`leaderboard_stale_gt_8d` above, not a live-trade feed artifact), or when `champion_after` itself is null (outcome=error, evaluation aborted before a champion could even be read). Optional (not in `required`) precisely so pre-I3165 v2 records remain valid without a version bump \u2014 per this schema's additive-only policy."
    },
    "arm_scores": {
      "type": [
        "object",
        "null"
      ],
      "description": "Every registered arm's score this week, keyed by arm name \u2014 the N-arm view (alpha-engine-config-I8756). `champion_score` and `challenger_score` remain the incumbent and the best challenger, so a two-arm reader is unaffected; this field is what makes a three-way week legible rather than collapsed into the pair that happened to matter. A null VALUE means that arm produced no comparable evidence this week.",
      "additionalProperties": {
        "type": [
          "number",
          "null"
        ]
      }
    }
  }
}
